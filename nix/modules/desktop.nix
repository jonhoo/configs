# Desktop-specific configuration (stationary machines)
{ config, pkgs, ... }:

{
  # Placeholder for future desktop-specific settings
  # Could include: multi-monitor defaults, no power management, etc.
}
